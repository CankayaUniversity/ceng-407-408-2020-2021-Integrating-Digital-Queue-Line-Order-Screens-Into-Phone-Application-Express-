<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.prototype.Express">
    <!-- CAMERA PERMISSION FOR QR -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Express"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.prototype.Express.Login"></activity>
        <activity android:name="com.prototype.Express.Register" />
        <activity android:name="com.prototype.Express.MainActivity"/>
        <activity android:name="com.prototype.Express.Profile_Activity" />
        <activity android:name="com.prototype.Express.QR_Activity" />
        <activity android:name="com.prototype.Express.Splash_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>